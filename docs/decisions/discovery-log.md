# 规格发现日志

## 2026-07-10：Phase 3.4 流程规格回填

### 范围

本次只整理 Markdown 规格，不修改 `src`、页面 UI 或业务逻辑。检查来源包括 `AGENTS.md`、`PRD.md`、`TASKS.md` 和当前预警管理类型、mock、抽屉按钮、反馈模块、时间线与默认表格实现。

### 问题分类

- A：领域定义，包括术语、状态、线索类型和业务不变量。
- B：角色与隐私，包括查看、判断、操作和通知权限。
- C：流程与状态流转，包括触发、允许操作、成功状态和禁止转移。
- D：数据语义，包括字段含义、已读状态、排序和派生标签。
- E：体验与展示，包括模块显隐、按钮、表格字段和交互文案。
- F：工程与交付，包括组件、mock/API、构建、Git、部署和测试。

### 已发现并在规格基线中统一的问题

| 分类 | 发现 | 基线处理 |
|---|---|---|
| A | `AGENTS.md` 把 `screening_abnormal` 与详情风险依据线索类型并列，和 PRD 后段规则冲突 | 详情风险依据线索类型统一为班主任上报、AI倾诉、深度测评；普筛异常只作为触发或时间线事件 |
| A/C | PRD 早期把“普筛异常”和“协作反馈中”写入核心状态链，与七种主处置状态和反馈状态独立建模冲突 | 领域规格将二者定义为流程事件或协作进度，不作为主处置状态 |
| D | “有反馈”和“有新反馈”容易混用 | 用 `feedbackRecords` 和 `hasUnreadFeedback` 明确派生规则 |
| E | PRD 早期低保真描述要求列表展示“下一步动作、负责心理老师”，与 AGENTS 和 PRD 实现原则冲突 | 规格基线固定默认列表七字段，禁止展示下一步动作和负责心理老师 |
| C/E | PRD 前方三处按钮表仅写“转介中：记录转介结果”，后方流程定义为“记录转介结果 / 安排复测” | 已按本轮明确要求统一三处按钮定义 |
| F | `TASKS.md` 所有阶段仍未勾选，且把状态变体放在 Phase 4 | 已按真实进度标记 Phase 1、Phase 2、高级筛选、七状态抽屉和 Phase 3.3 完成；Phase 4 改为抽屉操作后的本地 mock 状态变化 |

### Phase 3.4 待确认问题关闭状态

| 分类 | 原问题 | Phase 3.5 结论 | 状态 |
|---|---|---|---|
| A/C | 驳回后的持久化主状态 | 操作统一为“结束本次线索处理”，不新增第八种状态；本轮不实现归档流转 | 已关闭 |
| C | 记录转介结果后的主状态 | 保持转介中，不自动进入待复测 | 已关闭 |
| C/D | 班主任协作任务是否独立记录 | 合并到“确认正式预警”的描述中 | 已关闭 |
| B/C | 请求补充反馈是否可选择年级主任 | 只能选择班主任 | 已关闭 |
| A/C | AI倾诉是否允许直接入池 | 允许，定义为 `ai_chat_trigger` 第三条来源路径 | 已关闭 |
| B/C | 转介、复测状态更新和闭环后通知谁 | 通知对应班主任，年级主任暂不接收 | 已关闭 |

### Phase 3.4 当时的实现差异

- TypeScript `ClueType` 仍包含 `screening_abnormal`，但风险依据组件会回退展示为深度测评，mock 当前未使用该值。本轮禁止修改代码，因此只建立领域约束。
- 部分历史 mock 以“AI 生成风险线索”为起点，没有普筛或班主任上报入池节点。是否保留第三条直接入池路径待确认。
- 当前按钮均为占位反馈，尚未执行状态、最新动态、反馈状态或时间线的本地更新；该工作属于 Phase 4。

### 决策原则

在待确认问题得到产品结论前：

1. 不为缺失状态或通知对象自行造规则。
2. 不实现对应的本地 mock 状态变化。
3. 可继续做不依赖这些结论的展示验收和文档校对。
4. 确认后同步更新领域规格、流程规格、PRD、验收用例和 TASKS，再进入实现。

## 2026-07-10：Phase 3.5 预警流程模型校准

### 范围

先同步领域、流程、验收和页面 PRD，再按最新文档整改数据模型、mock 和详情抽屉。只授权“确认正式预警”的局部本地流转，其他按钮继续占位。

### 本轮发现

| 分类 | 发现 | 影响 | 本轮处理 |
|---|---|---|---|
| A/C | 线索池与待复核被混用 | 刚入池、尚未形成证据的事项被错误放进预警主状态 | 明确线索池是上游容器，待复核必须完成补充评估并形成风险依据 |
| A/D | 来源类型和风险依据被同一个 `clueType` 混用 | 普筛异常可能被当作详情证据，高级筛选语义不稳定 | 拆分为 `sourceType` 和 `evidenceTypes` |
| A/D | 系统建议风险等级和心理老师确认等级被同一个 `riskLevel` 混用 | 系统提示可能被误认为专业结论 | 拆分为 `suggestedRiskLevel` 和 `confirmedRiskLevel`，有效等级以后者优先 |
| C/E | 打开详情被误认为完成复核 | 时间线出现没有业务结果的“心理老师复核” | 只有提交三种复核结果才算完成，删除独立复核节点 |
| C/D | 时间线记录过多阅读动作和系统细节 | “查看复测结果”、独立协作任务等噪声掩盖业务结果 | 合并来源入池和正式预警事件，删除阅读节点，统一倒序 |
| D/E | 详情缺少干预记录和复测记录 | 处置过程只能从时间线推断，证据不足 | 新增结构化干预记录与复测记录模块，均按最新在前展示 |
| B | 年级主任权限边界不清楚 | 年级主任可能被错误允许提交事实观察或接收心理处置通知 | 限定为查看协作进度、督办和留痕，不提交班主任反馈，不接收本轮三类通知 |

### 已确认规则

- 三条上游来源路径为普筛异常、班主任上报和 AI倾诉主动触发。
- 正式预警前的深度测评和 AI倾诉属于补充评估，不是复测。
- 确认正式预警必须由心理老师选择正式风险等级；调整系统提示时必须填写理由。
- 确认正式预警与系统生成班主任协作任务合并为一条时间线记录。
- 请求补充反馈只能发给班主任。
- 记录转介结果后保持转介中。
- 发起转介、更新复测状态和完成闭环后通知对应班主任，年级主任暂不接收。
- 业务时间线、班主任反馈、干预记录和复测记录均最新在前。

### 本轮不实现

- 线索池页面和线索池进度交互。
- 真实后端、权限系统和小程序通知。
- 完整干预表单、完整复测结果页和完整归档页。
- 除确认正式预警外的完整本地状态流转。
